<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.13</version>
        <relativePath/>
    </parent>

    <groupId>com.newstrust</groupId>
    <artifactId>newstrust-backend</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>newstrust-backend</name>
    <description>NewsTrust - sistema de analise de credibilidade de noticias e fontes (Java/LangChain4j/RAG)</description>

    <properties>
        <java.version>25</java.version>
        <langchain4j.version>1.18.0</langchain4j.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>dev.langchain4j</groupId>
                <artifactId>langchain4j-bom</artifactId>
                <version>${langchain4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Persistence -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Registra o suporte a SqlTypes.VECTOR (usado por @JdbcTypeCode nas entidades
             com embedding) no dialeto PostgreSQL - hibernate-core sozinho NAO sabe
             renderizar o tipo "vector(n)", precisa deste modulo separado. Versao presa
             (nao gerenciada pelo BOM do Spring Boot) para bater exatamente com o
             hibernate-core que o starter de JPA resolve (6.6.39.Final). -->
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-vector</artifactId>
            <version>6.6.39.Final</version>
        </dependency>

        <!-- Migrations -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Monitoring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- LangChain4j - Google Gemini (API direta do Google AI Studio, sem projeto GCP/Vertex).
             Modulo estavel (1.18.0); a variante Vertex AI e o langchain4j-pgvector ainda estao em
             beta, por isso nao sao usados aqui - a busca de similaridade continua no adapter
             JPA/pgvector proprio, independente de qual biblioteca gerou o embedding. -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-google-ai-gemini</artifactId>
        </dependency>

        <!-- Documentacao da API (OpenAPI/Swagger UI) - 2.8.x e a linha compativel com
             Spring Boot 3.x; a serie 3.0.x do springdoc exige Spring Boot 4. -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.17</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Byte Buddy (usado pelo Mockito) so suporta oficialmente ate o Java 24;
                         esta flag habilita o modo experimental necessario para mocks em Java 25. -->
                    <systemPropertyVariables>
                        <net.bytebuddy.experimental>true</net.bytebuddy.experimental>
                    </systemPropertyVariables>
                    <!-- Java 9+ bloqueia por padrao um processo de anexar um agent (usado pelo
                         inline mock maker do Mockito/Byte Buddy) a si mesmo; sem isso o forked
                         JVM do Surefire falha com "Could not self-attach to current VM". -->
                    <argLine>-Djdk.attach.allowAttachSelf=true</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
